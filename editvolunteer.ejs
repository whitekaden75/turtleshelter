<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Volunteer</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <style>
        body, html {
            margin: 0;
            padding: 0;
            width: 100%;
            height: 100%;
        }

        /* Transparent Navigation Bar */
        .nav-bar {
            position: sticky;
            top: 0;
            z-index: 10;
            background-color: black;
            color: white;
            display: flex;
            justify-content: flex-end;
            align-items: center;
            padding: 10px 20px;
            height: 50px;
        }

        .nav-bar a {
            color: white;
            text-decoration: none;
            font-size: 14px;
            margin-left: 20px;
            font-weight: bold;
            text-transform: uppercase;
            transition: color 0.3s ease;
        }

        .nav-bar a:hover {
            color: lightgreen;
        }

        /* Form Styling */
        .form-container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #f9f9f9;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .form-container label {
            font-weight: bold;
            margin-top: 10px;
        }

        .form-container .btn {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            font-weight: bold;
            transition: background-color 0.3s ease, transform 0.2s ease;
            border: none;
            border-radius: 4px;
        }

        .form-container .btn-save {
            background-color: lightgreen;
            color: black;
        }

        .form-container .btn-save:hover {
            background-color: #89e589;
            transform: scale(1.02);
        }

        .form-container .btn-cancel {
            background-color: black;
            color: white;
            margin-top: 10px;
        }

        .form-container .btn-cancel:hover {
            background-color: #444;
            transform: scale(1.02);
        }
    </style>
</head>
<body>
    <!-- Transparent Navigation Bar -->
    <div class="nav-bar">
        <a href="/">Home</a>
        <a href="/viewvolunteers">Volunteers</a>
    </div>

    <h1 class="text-center mb-4">Edit Volunteer</h1>

    <!-- Form to edit volunteer details -->
    <div class="form-container">
        <form action="/editvolunteer/<%= Volunteer.VolunteerID %>" method="POST">
            <div class="mb-3">
                <label for="VolFirstName">First Name:</label>
                <input type="text" id="VolFirstName" name="VolFirstName" class="form-control" value="<%= Volunteer.VolFirstName %>" required>
            </div>
            <div class="mb-3">
                <label for="VolLastName">Last Name:</label>
                <input type="text" id="VolLastName" name="VolLastName" class="form-control" value="<%= Volunteer.VolLastName %>" required>
            </div>
            <div class="mb-3">
                <label for="VolEmail">Email:</label>
                <input type="email" id="VolEmail" name="VolEmail" class="form-control" value="<%= Volunteer.VolEmail %>" required>
            </div>
            <div class="mb-3">
                <label for="VolPhone">Phone:</label>
                <input type="text" id="VolPhone" name="VolPhone" class="form-control" value="<%= Volunteer.VolPhone %>" required>
            </div>
            <div class="mb-3">
                <label for="HoursAvailable">Hours Available:</label>
                <input type="number" id="HoursAvailable" name="HoursAvailable" class="form-control" value="<%= Volunteer.HoursAvailable %>" required>
            </div>
            <div class="mb-3">
                <label for="SewingLevel">Sewing Level:</label>
                <select id="SewingLevel" name="SewingLevel" class="form-select">
                    <option value="Beginner" <%= Volunteer.SewingLevel === "BEGINNER" ? "selected" : "" %>>BEGINNER</option>
                    <option value="Intermediate" <%= Volunteer.SewingLevel === "INTERMEDIATE" ? "selected" : "" %>>INTERMEDIATE</option>
                    <option value="Expert" <%= Volunteer.SewingLevel === "EXPERIENCED" ? "selected" : "" %>>EXPERIENCED</option>
                </select>
            </div>
            <div class="mb-3">
                <label for="ReferralSourceType">Referral Source:</label>
                <select id="ReferralSourceType" name="ReferralSourceType" class="form-select" required>
                    <option value="SOCIAL MEDIA" <%= Volunteer.ReferralSourceType === "SOCIAL MEDIA" ? "selected" : "" %>>SOCIAL MEDIA</option>
                    <option value="FRIEND RECOMMENDATION" <%= Volunteer.ReferralSourceType === "FRIEND RECOMMENDATION" ? "selected" : "" %>>FRIEND RECOMMENDATION</option>
                    <option value="LOCAL EVENT" <%= Volunteer.ReferralSourceType === "LOCAL EVENT" ? "selected" : "" %>>LOCAL EVENT</option>
                    <option value="OTHER" <%= Volunteer.ReferralSourceType === "OTHER" ? "selected" : "" %>>OTHER</option>
                </select>
            </div>
            <button type="submit" class="btn btn-save">Save Changes</button>
        </form>
        <div class="back-to-home-btn">
            <a href="/viewvolunteers" class="btn btn-cancel">Cancel</a>
        </div>
    </div>
</body>
</html>
