<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Volunteer</title>
    <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
    <style>
        .back-to-home-btn {
          position: absolute;
          right: 10px;
          top: 690px; /* Adjust the top margin as needed */
          padding: 10px;
        }
    </style>
    <!-- Header Section -->
    <header>
        <div class="left-align">TURTLE SHELTER <br />PROJECT</div>
        <nav class="right-align">
            <a href="/">HOME</a>
            <a href="/viewvolunteers">VOLUNTEERS</a>
        </nav>
    </header>

    <h1 style="text-align: center;" class="page-title">Edit Volunteer</h1>

    <!-- Form to edit volunteer details -->
    <div class="middlepage">
        <form action="/editvolunteer/<%= Volunteer.VolunteerID %>" method="POST" class="middlepage">
            <label for="VolFirstName">First Name:</label>
            <input type="text" id="VolFirstName" name="VolFirstName" value="<%= Volunteer.VolFirstName %>" required />
    
            <label for="VolLastName">Last Name:</label>
            <input type="text" id="VolLastName" name="VolLastName" value="<%= Volunteer.VolLastName %>" required />
    
            <label for="VolEmail">Email:</label>
            <input type="email" id="VolEmail" name="VolEmail" value="<%= Volunteer.VolEmail %>" required />
    
            <label for="VolPhone">Phone:</label>
            <input type="text" id="VolPhone" name="VolPhone" value="<%= Volunteer.VolPhone %>" required />
    
            <label for="HoursAvailable">Hours Available:</label>
            <input type="number" id="HoursAvailable" name="HoursAvailable" value="<%= Volunteer.HoursAvailable %>" required />
    
            <label for="SewingLevel">Sewing Level:</label>
            <select id="SewingLevel" name="SewingLevel">
                <option value="Beginner" <%= Volunteer.SewingLevel === "Beginner" ? "selected" : "" %>>Beginner</option>
                <option value="Intermediate" <%= Volunteer.SewingLevel === "Intermediate" ? "selected" : "" %>>Intermediate</option>
                <option value="Expert" <%= Volunteer.SewingLevel === "Expert" ? "selected" : "" %>>Expert</option>
            </select>
    
            <label for="ReferralSourceType">Referral Source:</label>
            <select id="ReferralSourceType" name="ReferralSourceType" required>
                <option value="SOCIAL MEDIA" <%= Volunteer.ReferralSourceType === "SOCIAL MEDIA" ? "selected" : "" %>>SOCIAL MEDIA</option>
                <option value="FRIEND RECOMMENDATION" <%= Volunteer.ReferralSourceType === "FRIEND RECOMMENDATION" ? "selected" : "" %>>FRIEND RECOMMENDATION</option>
                <option value="LOCAL EVENT" <%= Volunteer.ReferralSourceType === "LOCAL EVENT" ? "selected" : "" %>>LOCAL EVENT</option>
                <option value="OTHER" <%= Volunteer.ReferralSourceType === "OTHER" ? "selected" : "" %>>OTHER</option>
            </select>
    
            <form action="/editvolunteer/<%= Volunteer.VolunteerID %>" method="POST">
                <!-- Form fields go here -->
                <button class="btn" type="submit">Save Changes</button>
            </form>
            
        </form>
    
        <!-- Back to volunteers button -->
        <div class="back-to-home-btn" style="text-align: right; padding: 50px;">
            <a href="/viewvolunteers" class="btn">Cancel</a>
        </div>
    </div>
    </body>
</html>
