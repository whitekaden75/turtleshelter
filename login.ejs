<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Turtle Shelter Project</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <style>
        body, html {
            margin: 0;
            padding: 0;
            width: 100%;
            height: 100%;
        }

        /* Transparent Navigation Bar */
        .nav-bar {
            position: sticky;
            top: 0;
            z-index: 10;
            background-color: black;
            color: white;
            display: flex;
            justify-content: flex-end;
            align-items: center;
            padding: 10px 20px;
            height: 50px;
        }

        .nav-bar a {
            color: white;
            text-decoration: none;
            font-size: 14px;
            margin-left: 20px;
            font-weight: bold;
            text-transform: uppercase;
            transition: color 0.3s ease;
        }

        .nav-bar a:hover {
            color: lightgreen;
        }

        /* Login Form Styling */
        .login-container {
            max-width: 400px;
            margin: 50px auto;
            padding: 20px;
            background-color: #f9f9f9;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .login-container h2 {
            margin-bottom: 20px;
            font-weight: bold;
        }

        .login-container label {
            font-weight: bold;
            display: block;
            margin-top: 10px;
        }

        .login-container .inputbox {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .login-container .button {
            background-color: lightgreen;
            color: black;
            border: none;
            font-weight: bold;
            padding: 10px;
            width: 100%;
            border-radius: 4px;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .login-container .button:hover {
            background-color: #89e589;
            transform: scale(1.05);
        }

        /* Footer */
        footer {
            background-color: #343a40;
            color: white;
        }

        footer a {
            color: lightgray;
            text-decoration: none;
        }

        footer a:hover {
            color: white;
        }
    </style>
</head>
<body>
    <!-- Transparent Navigation Bar -->
    <div class="nav-bar">
        <a href="https://turtleshelterproject.org/about-homelessness-and-the-cold">Home</a>
        <a href="https://turtleshelterproject.org/jensstory">Jen's Story</a>
        <a href="https://turtleshelterproject.org/thank-you-to-our-sponsors">Sponsors</a>
        <a href="https://turtleshelterproject.org/about-homelessness-and-the-cold">About</a>
        <a href="https://turtleshelterproject.org/take-action">Take Action</a>
        <a href="/">Volunteer</a>
    </div>

    <!-- Login Form -->
    <div class="login-container">
        <form action="/login" method="POST" class="login-form">
            <h2>Login</h2>
            <label for="username">Username</label>
            <input
                class="inputbox"
                type="text"
                id="username"
                name="Username"
                placeholder="Enter your username"
                required>
            <label for="password">Password</label>
            <input
                class="inputbox"
                type="password"
                id="password"
                name="Password"
                placeholder="Enter your password"
                required>
            <button type="submit" class="button">Login</button>
        </form>
    </div>


</body>
</html>
